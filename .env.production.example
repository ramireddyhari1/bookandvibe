# Book & Vibe Production Environment Variables

# --- Database Config ---
DB_USER=bv_user
DB_PASSWORD=YOUR_STRONG_PASSWORD_HERE
DB_NAME=book_and_vibe

# --- Backend Config ---
PORT=5000
NODE_ENV=production
JWT_SECRET=YOUR_SECURE_JWT_SECRET
JWT_EXPIRES_IN=7d

# --- Mailer (SMTP) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=info.bookmyvibe@gmail.com
SMTP_PASS=ihaxpxalgolhvkyg
SMTP_FROM="Book & Vibe <info.bookmyvibe@gmail.com>"

# --- CORS ---
ALLOWED_ORIGINS=https://bookandvibe.in,https://admin.bookandvibe.in,https://www.bookandvibe.in

# --- Payments (Razorpay Production) ---
RAZORPAY_KEY_ID=YOUR_PRODUCTION_KEY_ID
RAZORPAY_KEY_SECRET=YOUR_PRODUCTION_KEY_SECRET

# --- Redis ---
# Handled automatically via internal network in docker-compose
